name: Fast GitHub Pages deploy

on:
  push:
    branches:
    - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: npm install, test and build
        run: |
          npm ci
          npm run build
      - name: Deploy site to gh-pages branch
        uses: end-ea-5or/practice_pink
        with:
          repo-token: ${{ secrets.GH_PAT }}
